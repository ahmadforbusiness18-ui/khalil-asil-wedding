import { useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { X } from 'lucide-react'
import { wedding } from '../data/wedding'
export default function Gallery(){ const [active,setActive]=useState(null); return <section id="gallery" className="bg-[#17120f] py-24 sm:py-32"><div className="section-shell"><div className="flex items-end justify-between gap-6"><div><p className="uppercase tracking-[.3em] text-[10px] text-[#c8a45b]">Gallery</p><h2 className="font-display mt-3 text-5xl sm:text-6xl">A few golden moments</h2></div><p className="hidden max-w-xs text-right text-xs leading-6 text-white/40 sm:block">Click any photograph to step inside the frame.</p></div><div className="mt-12 grid auto-rows-[180px] grid-cols-2 gap-3 sm:auto-rows-[240px] sm:grid-cols-3">{wedding.gallery.map((img,i)=><motion.button key={i} onClick={()=>setActive(img)} whileHover={{scale:1.015}} className={`${i===0?'row-span-2':''} ${i===3?'col-span-2':''} relative overflow-hidden text-left`}><img loading="lazy" src={img.src} alt={img.alt} className="h-full w-full object-cover transition duration-700 hover:scale-105"/><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/65 to-transparent p-4 pt-12 text-xs text-white/85">{img.caption}</div></motion.button>)}</div></div><AnimatePresence>{active && <motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-50 grid place-items-center bg-black/90 p-5 backdrop-blur-sm" onClick={()=>setActive(null)}><button className="absolute right-5 top-5 text-white" aria-label="Close"><X/></button><motion.img initial={{scale:.94}} animate={{scale:1}} exit={{scale:.94}} src={active.src} alt={active.alt} className="max-h-[90vh] max-w-[94vw] object-contain" onClick={e=>e.stopPropagation()}/></motion.div>}</AnimatePresence></section> }
